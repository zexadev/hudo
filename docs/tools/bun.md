# Bun

高性能 JavaScript/TypeScript 运行时与包管理器，兼容 Node.js API。

## 安装

```powershell
hudo install bun
```

安装到 `{install_root}\tools\bun\`。

## 安装后

```powershell
bun --version
bun run index.ts
bun install
```

## 卸载

```powershell
hudo uninstall bun
```
